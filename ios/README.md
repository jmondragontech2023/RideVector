# RideVector iOS (Milestone 0 placeholder)

Milestone 0 does **not** create an Xcode project (ADR-013).

## Toolchain verification (recorded)

Verified on a macOS contributor machine during Milestone 0 scaffold:

```text
Xcode 26.4 (Build version 17E192)
Apple Swift version 6.3
```

Re-check locally:

```bash
xcodebuild -version
swift --version
```

Do not add application source, schemes, or SPM packages here until a later milestone creates the real project.
