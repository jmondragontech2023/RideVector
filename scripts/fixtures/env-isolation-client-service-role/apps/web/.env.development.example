# Negative fixture: client example must not contain privileged secret markers.
# Value is a deterministic fake — not a real credential.
RIDEVECTOR_ENV=development
VITE_API_BASE_URL=https://ridevector-api-development.jmondragontech.workers.dev
SUPABASE_SERVICE_ROLE_KEY=fixture-not-a-real-service-role-key
