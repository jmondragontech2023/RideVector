# Local workspace defaults (no secrets).
RIDEVECTOR_ENV=local

# POC routing (Worker only — web talks to RideVector API, not Valhalla directly).
# Temporary public demo endpoint for development; override for self-hosted Valhalla later.
# VALHALLA_BASE_URL=https://valhalla1.openstreetmap.de
