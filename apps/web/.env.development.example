RIDEVECTOR_ENV=development
# Local Worker by default for day-to-day web + API pairing.
VITE_API_BASE_URL=http://127.0.0.1:8787
# Optional: development Worker URL after deploy (never production).
# VITE_API_BASE_URL=https://ridevector-api-development.jmondragontech.workers.dev
