# Example non-secret placeholders for apps/api (development remote)
# Worker secrets use: wrangler secret put --env development <NAME>
RIDEVECTOR_ENV=development
# Replace with the real development project URL after creation (never production).
SUPABASE_URL=https://REPLACE_ME_DEVELOPMENT_REF.supabase.co
