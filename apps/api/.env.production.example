# Production placeholders only — never commit real secrets.
# Deploy with: pnpm --filter @ridevector/api run deploy:production (explicit).
RIDEVECTOR_ENV=production
SUPABASE_URL=https://REPLACE_ME_PRODUCTION_REF.supabase.co
