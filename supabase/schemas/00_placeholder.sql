-- Milestone 0: declarative schema placeholder.
-- Product tables are introduced in later milestones.
-- Edit files under supabase/schemas/, then generate reviewed migrations with:
--   supabase db diff -f <name>
-- Do not enable RLS policies here until real tables exist.
select 1;
